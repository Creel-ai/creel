# Development

- Python version managed by **pyenv** (see `.python-version` — currently 3.12)
- Package management via **uv** (`uv pip install`)
- Virtual environment at `.venv` — always use `.venv/bin/python` to run tests
- Run tests: `.venv/bin/python -m pytest tests/ -v`
