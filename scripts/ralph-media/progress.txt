# Ralph Progress Log — Media Messages
Started: 2026-02-26
---

## Codebase Patterns
- **Channels:** src/taskrunner/channels/ — each channel extends Channel base class (base.py)
- **Channel base class:** listen(callback: Callable[[str, str], str]) and send(recipient, text)
- **Channel plugins:** register_plugin() returns (ChannelPluginMeta, factory) — see imessage.py for example
- **Channel registry:** src/taskrunner/channels/registry.py manages channel loading
- **ChatServer:** src/taskrunner/chat.py — handle_message(sender_id, text, ...) processes messages and calls LLM
- **DaemonService:** src/taskrunner/daemon/service.py bridges channels to ChatServer._handle_message
- **Models:** src/taskrunner/models.py — Pydantic models for all config (AgentConfig, LLMConfig, channel configs, etc.)
- **Existing channels:** WhatsApp (whatsapp.py), iMessage (imessage.py), BlueBubbles (bluebubbles.py)
- **iMessage:** polls ~/Library/Messages/chat.db SQLite, sends via AppleScript
- **WhatsApp:** uses WhatsAppBridge (HTTP or Neonize), supports polling and webhook modes
- **Telegram channel EXISTS:** src/taskrunner/channels/telegram.py (315 lines) + telegram_bridge.py (202 lines)
- **TelegramAttachment** model in telegram_bridge.py: file_id, file_type ("photo"/"document"/"voice"/"video"), mime_type, file_name
- **Bridge.download_file(file_id) -> bytes** already implemented
- **Bridge._parse_update()** already extracts text and captions from media messages
- DO NOT recreate the Telegram channel — extend it to pass attachments through to the agent
- **Tests:** run with `cd /Users/ross/.openclaw/workspace/projects/creel && uv run pytest tests/ -x -q`
- **LLM message format:** check chat.py for how messages are built — content is typically a string, needs to become content blocks for vision
- **Agent config:** agent.yaml loaded as AgentConfig Pydantic model
- **Outputs:** src/taskrunner/outputs.py has MESSAGE_PREFIX used for sent-message detection
- **Services dir:** src/taskrunner/services/ — may not exist yet, create if needed

## 2026-02-26 - MEDIA-001
- Implemented IncomingMessage and Attachment Pydantic models in src/taskrunner/channels/message.py
- Added AttachmentType StrEnum (image, voice, audio, video, file)
- Added wrap_legacy_callback() in src/taskrunner/channels/base.py — wraps (sender_id, text) callbacks to also accept IncomingMessage
- Updated ChatServer.handle_message to accept optional attachments keyword parameter
- Files changed: src/taskrunner/channels/message.py (new), src/taskrunner/channels/base.py, src/taskrunner/chat.py, tests/test_message_models.py (new)
- **Learnings:**
  - message.py was partially scaffolded from a previous run but lacked wrap_legacy_callback and ChatServer changes
  - The data field on Attachment uses exclude=True, repr=False to avoid serializing large binary blobs
  - DaemonService.send_message routes to ChatServer.handle_message — no changes needed there for this story
  - All 1797 existing tests pass + 15 new tests
---

## 2026-02-26 - MEDIA-002
- Implemented MediaStore class in src/taskrunner/services/media_store.py
- Created src/taskrunner/services/ package with __init__.py
- MediaStore features: save from bytes/file_path/URL, SHA-256 deduplication, configurable retention cleanup, organized file structure ({channel}/{YYYY-MM-DD}/{uuid}.{ext})
- URL downloads use httpx streaming with max file size enforcement (20MB default)
- MIME-to-extension mapping for common image/audio/video types with fallback chain (file_name → mime_type → attachment type)
- Files changed: src/taskrunner/services/__init__.py (new), src/taskrunner/services/media_store.py (new), tests/test_media_store.py (new)
- **Learnings:**
  - httpx is already a project dependency — used streaming download to enforce size limits without loading entire file
  - Deduplication uses in-memory hash index (populated on save) — sufficient for session-scoped use, doesn't persist across restarts
  - Cleanup prunes empty directories bottom-up after file removal
  - All 1824 tests pass (1797 existing + 27 new)
---

## 2026-02-26 - MEDIA-003
- Implemented TranscriptionService class in src/taskrunner/services/transcription.py
- Two backends: "openai" (Whisper API via httpx POST) and "local" (whisper CLI with API fallback)
- Audio format conversion: unsupported formats (.caf, .opus, .amr, .aac) converted to .wav via ffmpeg when available
- API key resolution: explicit api_key param → OPENAI_API_KEY env var (matches project's secrets-to-env pattern)
- Graceful failure: returns empty string with warning log on any error (file missing, API error, no key, etc.)
- Files changed: src/taskrunner/services/transcription.py (new), tests/test_transcription.py (new)
- **Learnings:**
  - LLMConfig has no api_key or provider field — credentials come from encrypted env files decrypted at runtime
  - TranscriptionService uses explicit key param or OPENAI_API_KEY from env, avoiding coupling to the secrets mechanism
  - Telegram voice messages are .oga/.ogg (natively supported by Whisper API), iMessage voice is .caf (needs ffmpeg conversion)
  - WHISPER_SUPPORTED_FORMATS constant exported for use by other services that need to check format compatibility
  - All 1843 tests pass (1824 existing + 19 new)
---

## 2026-02-26 - MEDIA-004
- Implemented VisionProcessor class in src/taskrunner/services/vision.py
- Supports two LLM provider formats: Anthropic (type: image, source: base64) and OpenAI (type: image_url, data URI)
- Image resizing: uses Pillow to downscale images exceeding max_pixels (default 2048) while preserving aspect ratio
- Format conversion: unsupported formats (HEIC, HEIF, TIFF, BMP) converted to JPEG via Pillow
- RGBA-to-RGB conversion for JPEG compatibility (strips alpha channel)
- Graceful degradation: without Pillow, supported formats (PNG, JPEG, GIF, WEBP) are sent as raw bytes; conversion-needed formats return None with warning
- Configurable quality parameter for JPEG compression
- Added Pillow>=10.0.0 as optional dependency under [project.optional-dependencies] vision group
- Files changed: src/taskrunner/services/vision.py (new), tests/test_vision.py (new), pyproject.toml (vision optional dep)
- **Learnings:**
  - LLM uses Anthropic API exclusively (llm.py only imports anthropic) — but VisionProcessor supports both formats for future flexibility
  - Pillow is optional — the service works without it for natively-supported formats, only needed for resize/conversion
  - _detect_mime_type uses a hardcoded map for common types, falls back to mimetypes module
  - Test helpers (_make_png, _make_jpeg, _make_bmp) create real image files via Pillow for realistic testing
  - All 1878 tests pass (1843 existing + 35 new)
---

## 2026-02-26 - MEDIA-006
- Extended IMessageChannel._poll to query attachment table via message_attachment_join
- SQL uses DISTINCT + LEFT JOIN to message_attachment_join so messages with attachments (even without text) are returned
- Added _query_attachments static method: queries attachment table for a message ROWID, resolves ~ paths, detects attachment type from MIME
- Attachment type detection: image/ → IMAGE, audio/ → AUDIO, voice MIME types (audio/x-caf, audio/caf, audio/amr) → VOICE, video/ → VIDEO, else FILE
- Tilde expansion: chat.db stores paths like ~/Library/Messages/Attachments/..., replaced with Path.home()
- Missing files: logged as warning, attachment still returned with file_path=None (skipped only if also no transfer_name)
- listen() builds IncomingMessage when attachments present, calls callback(incoming_message); text-only uses callback(sender_id, text)
- Updated DaemonService.send_message to accept IncomingMessage as first arg, forwarding attachments to ChatServer.handle_message
- Files changed: src/taskrunner/channels/imessage.py, src/taskrunner/daemon/service.py, tests/test_imessage_media.py (new)
- **Learnings:**
  - chat.db message_attachment_join uses (message_id, attachment_id) — must use DISTINCT in message query to avoid duplicates for multi-attachment messages
  - iMessage voice messages are .caf format (audio/x-caf), not in Whisper's supported formats — conversion handled by TranscriptionService from MEDIA-003
  - DaemonService.send_message needed signature update to accept IncomingMessage — used isinstance check for backward compat
  - Attachment files may be cleaned up by macOS — file_path=None is valid state, downstream processors should handle it
  - All 1902 tests pass (1878 existing + 24 new)
---

## 2026-02-26 - MEDIA-007
- Updated ChatServer to process media attachments before sending to LLM
- Added _process_attachments, _process_voice_attachment, _process_image_attachment methods to ChatServer
- Initialized MediaStore, TranscriptionService, VisionProcessor in ChatServer.__init__
- Voice/audio attachments: saved to media store → transcribed → prepended as "[Voice message]: {text}" to user message
- Image attachments: saved to media store → processed by VisionProcessor → added as content blocks to user message
- When images present: user message content changes from string to list of content blocks [{type: "text", text: ...}, {type: "image", source: ...}]
- Added SessionManager.add_user_message_blocks() for content-block user messages (preserves title extraction from text blocks)
- Graceful failure: voice save failure → "[Voice message: could not save audio file]", transcription failure → "[Voice message: transcription failed]", image processing failure → silently omitted
- Files changed: src/taskrunner/chat.py, src/taskrunner/session.py, tests/test_chat_media.py (new)
- **Learnings:**
  - Anthropic API accepts user message content as either str or list[dict] — content blocks are [{type: "text", text: ...}, {type: "image", ...}]
  - The LLM uses Anthropic exclusively (llm.py only imports anthropic), so VisionProcessor provider is "anthropic"
  - SessionManager.add_user_message stores content as string; new add_user_message_blocks stores as list for multi-modal
  - run_agent_loop receives messages list directly from session — content blocks flow through without any changes needed in agent.py
  - All 1918 tests pass (1902 existing + 16 new)
---

## 2026-02-26 - MEDIA-008
- Added MediaConfig, TranscriptionConfig, VisionConfig Pydantic models to src/taskrunner/models.py
- Added optional media: MediaConfig | None field to AgentDefinition
- Updated ChatServer.__init__ to use MediaConfig values when initializing MediaStore, TranscriptionService, VisionProcessor
- When media is None or media.enabled is False: services are set to None, attachments silently ignored with log message
- When media is enabled: storage_dir is tilde-expanded, max_file_size converted from MB to bytes, all config values passed through
- Added commented-out media section to agent.yaml as a template with sensible defaults
- TranscriptionService reads api_key from media.transcription.api_key, falling back to OPENAI_API_KEY env var (unchanged)
- Updated test_chat_media.py helper to include MediaConfig(enabled=True) so media services are initialized for attachment tests
- Files changed: src/taskrunner/models.py, src/taskrunner/chat.py, agent.yaml, tests/test_media_config.py (new), tests/test_chat_media.py
- **Learnings:**
  - AgentDefinition already has many optional config sections (guardian, browser, bridge) — media follows the same None-means-disabled pattern
  - ChatServer text-only tests don't need media config — _process_attachments returns early when attachments list is empty, before checking if services are None
  - Path.expanduser() handles ~ expansion for storage_dir
  - All 1935 tests pass (1918 existing + 17 new)
---
