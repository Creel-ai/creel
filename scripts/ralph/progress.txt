# Ralph Progress Log
Started: 2026-02-25
---

## Codebase Patterns
- Python source lives in src/taskrunner/ (package name is taskrunner, not creel yet)
- FastAPI daemon API is in src/taskrunner/daemon/api.py — use APIRouter for new endpoint groups
- Task models and loading in src/taskrunner/models.py — use load_all_tasks()
- Scheduler is APScheduler-based in src/taskrunner/scheduler.py
- NEW: Full cron system in src/taskrunner/cron/ (manager, store, models, executor, delivery, tool)
- Cron store already persists run history — check src/taskrunner/cron/store.py before building new history storage
- Cron API router in src/taskrunner/daemon/api_cron.py — prefix="/api/cron", mounted via `app.include_router(cron_router)` in api.py
- `_cron_to_human()` in api_cron.py converts common cron expressions to readable strings (every minute, daily at X, weekly on Y, etc.)
- Cron history read pattern: `_read_all_history()` reads all JSONL lines, filtering/pagination done in-memory
- Toggle pattern: regex-replace `enabled:` line in raw YAML, same as task toggle in api_tasks.py
- Config is agent.yaml (YAML), loaded as Pydantic models
- Telegram channel support added in src/taskrunner/channels/telegram.py
- Daemon service layer in src/taskrunner/daemon/service.py wraps business logic
- CREEL_HOME defaults to ~/.creel/ — state dir for daemon.sock, daemon.pid, daemon.log
- Tasks dir configurable, defaults to tasks/ relative to working directory
- Existing tests in tests/ — run with pytest
- pyproject.toml uses hatchling build system
- Bridge (FastAPI host bridge) is in src/bridge/ — separate from daemon
- Dashboard static dir path: `_DASHBOARD_STATIC_DIR` computed from `Path(__file__).resolve().parent.parent / "dashboard_static"` in api.py
- SPA fallback uses catch-all `/{full_path:path}` route — must be registered AFTER all other routes
- `_mount_dashboard()` is a no-op when dashboard_static/ dir is missing (graceful degradation)
- Tests run with `.venv/bin/python -m pytest tests/ -x -q`
- Dashboard scaffold: Vite 7 + React 19 + TypeScript 5.9 (strict mode enabled by default in template)
- Build script: scripts/build-dashboard.sh — npm ci + npm run build + copy dist/ to dashboard_static/
- Vite dev proxy: /api/* and /v1/* proxied to localhost:8099
- MUI v6 + Emotion installed; ThemeProvider in dashboard/src/ThemeContext.tsx wraps the app
- Dark mode toggle persists to localStorage key `creel-theme`; use `useThemeMode()` hook for mode/toggle
- Theme factory in dashboard/src/theme.ts — `buildTheme(mode)` creates MUI theme
- React Router v6 in dashboard/src/App.tsx — routes defined as children of Layout route
- BrowserRouter wraps the app in main.tsx
- Layout component in dashboard/src/Layout.tsx — AppBar (fixed) + responsive Drawer + Outlet
- Sidebar nav groups: Control, Workspace, System — defined as `NAV_GROUPS` array in Layout.tsx
- Page components in dashboard/src/pages/ — one file per page (OverviewPage.tsx, etc.)
- Drawer width: 240px; mobile breakpoint: `theme.breakpoints.down('md')`
- Dashboard API router in src/taskrunner/daemon/api_dashboard.py — prefix="/api", mounted via `app.include_router(dashboard_router)` in api.py
- Access DaemonService in endpoint handlers via `request.app.state.service`
- Agent definition accessible via `service._agent_def` (AgentDefinition model)
- TypeScript `erasableSyntaxOnly` disallows `public` param properties — use explicit field declarations
- MUI Grid v2 uses `size` prop: `<Grid size={{ xs: 12, md: 4 }}>` (not `xs={12} md={4}`)
- API client: dashboard/src/api/client.ts — typed fetch wrapper, add new API methods here
- Types for API responses defined in client.ts alongside fetch methods
- Task CRUD API router in src/taskrunner/daemon/api_tasks.py — prefix="/api/tasks", mounted via `app.include_router(tasks_router)` in api.py
- Task CRUD supports both form-field requests and raw_yaml passthrough for create/update
- Soft-delete pattern: move files to .deleted/ subdir instead of permanent removal
- Task toggle pattern: read raw_yaml via GET /api/tasks/{name}, regex-replace `enabled:` line, PUT back with raw_yaml field
- Types for API responses defined in dashboard/src/api/client.ts: TaskSummary, TaskDetail, TaskUpdateRequest
- CodeMirror 6 packages: codemirror (basic setup), @codemirror/state, @codemirror/view, @codemirror/lang-yaml, @codemirror/theme-one-dark
- js-yaml for YAML↔JSON conversion in browser; @types/js-yaml for TypeScript
- CodeMirror editor must be recreated when dark mode changes — theme is set at EditorState creation time
- Form↔YAML sync pattern: only convert on tab switch (not per-keystroke) to avoid feedback loops
- TaskEditPage serves both /tasks/new (create) and /tasks/:name (edit) — `useParams().name` determines mode
- MUI ToggleButton `value` must not be `null` — use sentinel string (e.g. 'all') instead
- Cron API types in client.ts: CronJob, CronRunRecord, CronHistoryResponse; methods: fetchCronJobs(), toggleCronJob(), fetchCronHistory(), fetchCronRunDetail()
- Expandable table rows pattern: wrap data row + collapse row in `<Box component="tbody">`, Collapse in a colSpan cell

---

## 2026-02-25 - DASH-001
- Implemented static SPA serving from FastAPI daemon
- Files changed: src/taskrunner/daemon/api.py
- Added imports: Path, Request, FileResponse, StaticFiles
- Added `_DASHBOARD_STATIC_DIR` module-level constant
- Added `_mount_dashboard()` helper that:
  - Checks if dashboard_static/ dir and index.html exist before mounting
  - Mounts /assets via StaticFiles if assets/ subdir exists
  - Registers catch-all GET route for SPA fallback
  - Serves actual static files if they exist, otherwise returns index.html
- Called at end of `create_daemon_app()` after all API routes are registered
- **Learnings for future iterations:**
  - The catch-all route must be registered last so it doesn't shadow API routes
  - When dashboard_static/ is missing, all existing endpoints work unchanged
  - FastAPI StaticFiles requires the directory to exist at mount time, so we guard with is_dir()
  - All 1373 existing tests pass with no changes needed
---

## 2026-02-25 - DASH-002
- Scaffolded Vite + React + TypeScript frontend in dashboard/
- Files created: dashboard/ (Vite scaffold), scripts/build-dashboard.sh
- Files changed: .gitignore, vite.config.ts
- Scaffold created with `npm create vite@latest -- --template react-ts`
- Simplified App.tsx to minimal Creel Dashboard placeholder, removed template boilerplate (App.css, index.css, assets/)
- Configured Vite dev proxy: /api/* and /v1/* → localhost:8099
- TypeScript strict mode already enabled by Vite react-ts template in tsconfig.app.json
- Build script runs: npm ci → npm run build → copy dist/ to src/taskrunner/dashboard_static/
- Added dashboard/node_modules/, dashboard/dist/, src/taskrunner/dashboard_static/ to .gitignore
- npm run build produces output in dashboard/dist/ (verified)
- Build script copies to src/taskrunner/dashboard_static/ (verified)
- **Learnings for future iterations:**
  - Vite 7 react-ts template uses React 19 (not 18) — fine for our purposes
  - TypeScript strict mode is on by default in the template's tsconfig.app.json
  - `npm ci` is used in build script (not `npm install`) for reproducible installs
  - The `python -m pytest` command needs `.venv/bin/python` prefix (no system python)
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-003
- Installed MUI v6: @mui/material, @mui/icons-material, @emotion/react, @emotion/styled
- Created `dashboard/src/theme.ts` — `buildTheme(mode)` factory using `createTheme()` with primary/secondary palette colors and dark background overrides
- Created `dashboard/src/ThemeContext.tsx` — React context providing `mode` and `toggleTheme()`, wraps MuiThemeProvider + CssBaseline, persists preference to localStorage key `creel-theme`
- Updated `dashboard/src/main.tsx` — wraps `<App />` in `<ThemeProvider>`
- Updated `dashboard/src/App.tsx` — MUI AppBar with "Creel Dashboard" title and dark mode toggle IconButton (Brightness4/7 icons)
- Files created: dashboard/src/theme.ts, dashboard/src/ThemeContext.tsx
- Files changed: dashboard/package.json, dashboard/src/main.tsx, dashboard/src/App.tsx
- **Learnings for future iterations:**
  - MUI v6 installed fine alongside React 19
  - Use `<ThemeContext value={...}>` (React 19 style) instead of `<ThemeContext.Provider value={...}>`
  - CssBaseline in the ThemeProvider ensures consistent baseline styling in both modes
  - `npm run build` produces 316 KB gzipped bundle (101 KB gzipped) with MUI included
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-004
- Implemented layout shell with sidebar navigation and React Router
- Files created: dashboard/src/Layout.tsx, dashboard/src/pages/OverviewPage.tsx, TaskListPage.tsx, TaskDetailPage.tsx, TaskNewPage.tsx, CronPage.tsx, FilesPage.tsx, ConfigPage.tsx, LogsPage.tsx, NotFoundPage.tsx
- Files changed: dashboard/src/App.tsx, dashboard/src/main.tsx, dashboard/package.json
- Installed react-router-dom dependency
- Layout component with:
  - Fixed AppBar with "Creel" name, connection status Chip (green "Connected"), dark mode toggle
  - Permanent MUI Drawer (240px) on desktop, temporary Drawer on mobile (< md breakpoint)
  - Sidebar nav grouped: Control (Overview, Tasks, Cron Jobs), Workspace (Files, Config), System (Logs)
  - Active page highlighted via `selected` prop on ListItemButton with custom `isActive()` helper
  - `useMediaQuery` for responsive drawer variant switching
- React Router v6 routes: /, /tasks, /tasks/new, /tasks/:name, /cron, /files, /config, /logs, * (404)
- BrowserRouter wraps the entire app in main.tsx
- 404 page with "Go to Overview" button
- All placeholder pages render their page title as h4 Typography
- **Learnings for future iterations:**
  - React Router `<Route path="tasks/new">` must come before `<Route path="tasks/:name">` to avoid matching "new" as a name param
  - NavLink `selected` state needs custom logic — use `isActive()` helper checking pathname prefix for nested routes (e.g., /tasks/*)
  - AppBar `position="fixed"` with `zIndex: theme.zIndex.drawer + 1` keeps it above the drawer
  - Need `<Toolbar />` spacer in both drawer content and main content to offset fixed AppBar height
  - `npm run build` succeeds — bundle is 415 KB (132 KB gzipped) with router + layout added
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-005
- Implemented GET /api/status endpoint returning daemon status, agent info, channels, task stats, cron info, and recent runs
- Files created: src/taskrunner/daemon/api_dashboard.py
- Files changed: src/taskrunner/daemon/api.py (imported and included dashboard_router)
- Created APIRouter with prefix="/api" in api_dashboard.py
- Endpoint reads from DaemonService.status(), agent_def, load_all_tasks(), and cron-history.jsonl
- Returns structured JSON: daemon (running, pid, uptime_seconds, socket), agent (name, model, provider), channels (with enabled/connected), tasks (total, scheduled), cron (enabled_jobs, total_jobs, next_run), recent_runs (last 5)
- Sensible defaults when no tasks/channels/history exist
- Router included in create_daemon_app() BEFORE webhook routes and SPA fallback
- **Learnings for future iterations:**
  - Dashboard API routes use `/api/*` prefix (via APIRouter prefix="/api"), distinct from existing `/v1/*` routes
  - The dashboard router must be included before `_mount_dashboard()` call since the SPA catch-all would shadow `/api/*` routes
  - `request.app.state.service` gives access to the DaemonService instance (set in lifespan)
  - `service._agent_def` provides agent config (name, model, provider)
  - Cron history JSONL path: `~/.creel/cron-history.jsonl` — doesn't exist yet (DASH-010 will create it)
  - `load_all_tasks()` can throw FileNotFoundError if tasks dir doesn't exist — wrap in try/except
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-006
- Implemented Overview page with live daemon status, agent info, channels, quick stats, and recent runs
- Files created: dashboard/src/api/client.ts (fetch wrapper with typed StatusResponse)
- Files changed: dashboard/src/pages/OverviewPage.tsx (full implementation replacing placeholder)
- API client in dashboard/src/api/client.ts: typed fetch wrapper with ApiError class, fetchStatus() method
- OverviewPage features:
  - Fetches GET /api/status on mount and displays data in MUI Card components
  - Daemon status card: running/stopped Chip badge, PID, human-readable uptime, socket path
  - Agent info card: name, model, provider
  - Channels section: list of channels with green/red outlined Chip badges
  - Quick stats card: total tasks, scheduled tasks, enabled cron jobs, next run
  - Recent runs: MUI Table with status Chips (success=green, failed=red, timeout=orange), relative time, duration
  - Auto-refreshes every 30 seconds via setInterval polling
  - Loading skeleton (Skeleton components in Card layout) while fetching
  - Error state: Alert component when daemon unreachable; warning Alert on refresh failures
- **Learnings for future iterations:**
  - TypeScript `erasableSyntaxOnly` (tsconfig.app.json) disallows `public` parameter properties in constructors — use explicit field declarations instead
  - MUI Grid v2 uses `size` prop instead of `xs`/`md` — e.g. `<Grid size={{ xs: 12, md: 4 }}>`
  - `npm run build` produces 442 KB (139 KB gzipped) with overview page added
  - API client lives in dashboard/src/api/client.ts — add new API methods here as needed
  - pytest needs full path: `cd /Users/ross/.openclaw/workspace/projects/creel && .venv/bin/python -m pytest tests/ -x -q`
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-007
- Implemented Task CRUD API endpoints: list, get, create, update, delete, run
- Files created: src/taskrunner/daemon/api_tasks.py
- Files changed: src/taskrunner/daemon/api.py (imported and included tasks_router)
- Endpoints:
  - GET /api/tasks — lists all tasks with name, description, schedule, enabled, last_modified, file_path
  - GET /api/tasks/{name} — full task detail with raw_yaml field
  - POST /api/tasks — create new task, validates with TaskDefinition Pydantic model, 409 if exists
  - PUT /api/tasks/{name} — update task, creates .bak backup before overwrite, 404 if not found
  - DELETE /api/tasks/{name} — soft-delete to .deleted/ subdirectory, 404 if not found
  - POST /api/tasks/{name}/run — triggers run_task() in background daemon thread, returns 202 with run_id
- Supports both form-field based creation and raw_yaml passthrough
- Uses same `_get_tasks_dir()` pattern from api_dashboard.py
- **Learnings for future iterations:**
  - Task CRUD router uses prefix="/api/tasks" (separate APIRouter from dashboard_router)
  - `_build_task_yaml()` constructs dict from form fields; `raw_yaml` field allows direct YAML editing
  - Soft-delete uses .deleted/ subdir with timestamp suffix for name collisions
  - Background task execution via threading.Thread(daemon=True) — fire-and-forget with orchestrator logging
  - Pydantic validation catches invalid task definitions before writing to disk
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-008
- Implemented Task list page with MUI Table, search, sort, and enable/disable toggles
- Files changed: dashboard/src/pages/TaskListPage.tsx (full implementation), dashboard/src/api/client.ts (added task API methods)
- Features:
  - MUI Table with columns: Name (bold, clickable → /tasks/{name}), Description, Schedule (human-readable), Last Modified (relative), Enabled (Switch toggle)
  - Enabled toggle uses `toggleTaskEnabled()` — reads raw YAML, modifies enabled field, PUTs back to preserve all other fields
  - TextField search filters tasks by name/description (client-side)
  - Sortable columns: Name, Schedule, Last Modified via TableSortLabel
  - New Task button (AddIcon) in top-right navigates to /tasks/new
  - Empty state: "No tasks yet. Create your first task." with button
  - Loading skeleton while fetching
  - Error handling for both initial load failures and toggle failures
- API client additions: TaskDetail interface, TaskSummary interface, TaskUpdateRequest interface, fetchTasks(), fetchTaskDetail(), updateTask(), toggleTaskEnabled()
- **Learnings for future iterations:**
  - PUT /api/tasks/{name} replaces the entire file — for partial updates like toggle, must read raw_yaml first, modify it, then send back with raw_yaml field
  - `toggleTaskEnabled()` reads task detail, regex-replaces `enabled:` line in YAML, then PUTs back — avoids losing other fields
  - The `stopPropagation()` on the Switch's parent TableCell prevents row click from navigating when toggling
  - `npm run build` produces 509 KB (158 KB gzipped) with task list page
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-009
- Implemented Task edit and create page with form mode and raw YAML editing
- Files created: dashboard/src/pages/TaskEditPage.tsx
- Files changed: dashboard/src/App.tsx (routing to TaskEditPage), dashboard/src/api/client.ts (added createTask, deleteTask, runTask methods), dashboard/package.json (new deps)
- New npm dependencies: @codemirror/lang-yaml, @codemirror/view, @codemirror/state, @codemirror/commands, @codemirror/language, @codemirror/theme-one-dark, codemirror, js-yaml, @types/js-yaml
- Features:
  - Form mode with MUI fields: Name (slug-validated), Description (multiline), Schedule (cron with human-readable preview), Prompt (multiline, required), Model override, Max tokens, Timeout, Output type (Select: stdout/imessage/file), Output target, Enabled (Checkbox)
  - Raw YAML tab with CodeMirror 6 editor: YAML syntax highlighting, line numbers, one-dark theme for dark mode
  - MUI Tabs to switch between Form and Raw modes
  - Tab switching syncs content: form→raw serializes via js-yaml, raw→form parses YAML back to form fields
  - Save button calls POST (create) or PUT (edit) — Snackbar success toast
  - Save disabled when no changes (dirty tracking via rawYaml vs savedYaml comparison)
  - Run Now button (edit mode only) triggers POST /api/tasks/{name}/run
  - Delete button (edit mode only) with Dialog confirmation
  - Form validation: required fields show error state, slug pattern validated on name
  - Unsaved changes warning via beforeunload event
  - Schedule preset helper: Select dropdown with common schedules (hourly, daily 9am, weekly Monday)
  - After creating a new task, navigates to /tasks/{name} (edit mode)
- API client additions: createTask(), deleteTask(), runTask()
- Replaced TaskDetailPage and TaskNewPage placeholders with single TaskEditPage
- **Learnings for future iterations:**
  - CodeMirror 6 requires `codemirror` (basic setup bundle), `@codemirror/state`, `@codemirror/view`, `@codemirror/lang-yaml`, and `@codemirror/theme-one-dark` as separate packages
  - Form↔YAML sync should only happen on tab switch, not on every keystroke, to avoid loops
  - The `internalUpdate` ref pattern prevents the external value sync from firing when the change originated from the editor itself
  - CodeMirror editor must be recreated when dark mode changes (theme is set at creation time)
  - `erasableSyntaxOnly` in tsconfig means no `enum` syntax — use plain objects/arrays for constants
  - js-yaml `dump()` with `lineWidth: -1` prevents unwanted line wrapping in generated YAML
  - Dirty tracking for edit mode compares current rawYaml against savedYaml snapshot; for create mode, checks if name or prompt are non-empty
  - `npm run build` produces 1016 KB (324 KB gzipped) — CodeMirror adds significant bundle size
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-010
- Implemented cron history storage: after each scheduled task execution, a JSONL record is appended to ~/.creel/cron-history.jsonl
- Files changed: src/taskrunner/scheduler.py
- Added `_cron_history_path()` — resolves JSONL path from CREEL_HOME env var (defaults to ~/.creel/)
- Added `_append_cron_history(record)` — appends JSON record to JSONL file, creates file/dirs if missing, catches OSError gracefully
- Modified `_run_task_safe()` to:
  - Generate UUID4 run_id before execution
  - Record started_at (UTC ISO timestamp) and start_time (monotonic) before calling run_task()
  - Capture result summary (first 500 chars) on success
  - Capture traceback string on failure
  - Compute duration_ms from monotonic clock
  - Write record: {run_id, job_name, started_at, finished_at, duration_ms, status, token_usage, error, summary}
- token_usage is None for now (orchestrator doesn't expose token counts yet)
- Record format matches what api_dashboard.py already reads via _read_recent_runs()
- **Learnings for future iterations:**
  - The existing `_read_recent_runs()` in api_dashboard.py already reads from the JSONL file — no changes needed there
  - `_cron_history_path()` is now defined in both scheduler.py and api_dashboard.py — could be refactored to a shared util later
  - All existing `_run_task_safe` tests still pass because `_append_cron_history` writes to `~/.creel/` without needing mocking
  - `time.monotonic()` is more reliable than `time.time()` for duration measurement (not affected by system clock changes)
  - job_name is derived from `Path(task_path).stem` which strips the directory and .yaml extension
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-011
- Implemented Cron API endpoints: list jobs, toggle, paginated history, run detail
- Files created: src/taskrunner/daemon/api_cron.py
- Files changed: src/taskrunner/daemon/api.py (imported and included cron_router)
- Endpoints:
  - GET /api/cron/jobs — lists tasks with schedule field: name, schedule, schedule_human, next_run, last_run, last_status, enabled
  - POST /api/cron/jobs/{name}/toggle — flips enabled field in YAML with .bak backup
  - GET /api/cron/history — paginated history with filters: ?job=name&status=success|failed|timeout&limit=50&offset=0
  - GET /api/cron/history/{run_id} — full detail of a single run
- `_cron_to_human()` converts common cron patterns: every minute, every N minutes, hourly, daily at HH:MM, weekly on day, monthly on Nth
- History read uses in-memory filtering and reverse-chronological ordering
- Toggle uses regex replacement of `enabled:` line in raw YAML (same pattern as task toggle)
- Returns empty lists gracefully when no history file exists
- **Learnings for future iterations:**
  - The `_cron_to_human()` function handles most common patterns but falls back to raw expression for complex ones
  - `next_run` is always None — would need APScheduler instance access to compute real next-run times
  - The `_get_tasks_dir()` and `_cron_history_path()` helpers are duplicated across api_dashboard.py, api_tasks.py, and api_cron.py — candidate for shared util
  - History pagination: all records loaded into memory then sliced — fine for typical JSONL sizes but could be optimized for very large files
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-012
- Implemented Cron jobs page with job list panel and run history panel
- Files changed: dashboard/src/pages/CronPage.tsx (full implementation), dashboard/src/api/client.ts (added cron API methods)
- API client additions: CronJob, CronRunRecord, CronHistoryResponse interfaces, fetchCronJobs(), toggleCronJob(), fetchCronHistory(), fetchCronRunDetail()
- Job list panel features:
  - MUI Table with columns: Name, Schedule (human-readable), Next Run, Last Run, Status (Chip), Enabled (Switch toggle)
  - Sort by next run (default), name via TableSortLabel
  - Filter dropdown: All/Enabled/Disabled via Select
  - Search by name via TextField
  - Click job row to filter run history panel below (highlighted with action.selected)
  - Toggle enabled via POST /api/cron/jobs/{name}/toggle
  - Empty state with create button
- Run history panel features:
  - MUI Table with Job Name, Started (relative), Duration, Status Chip, Summary (truncated)
  - Filter by status: ToggleButtonGroup (All/Success/Failed/Timeout)
  - Click row to expand via MUI Collapse showing full details: run ID, timestamps, duration, tokens, summary, error
  - Pagination: 'Load more' button at bottom, default 50 rows
  - Selected job shown as deletable Chip in header
- New Job dialog (MUI Dialog):
  - Name (slug-validated), schedule type (Select: preset or cron expression), interval preset or cron input with human preview, prompt (multiline), model override, timeout, enabled checkbox, Create button
  - Creates task via POST /api/tasks with raw YAML
- Empty states for both panels
- **Learnings for future iterations:**
  - MUI ToggleButton `value` must not be `null` — use string like 'all' instead of null for the default/unset state
  - `Box component="tbody"` can be used to group adjacent TableRows (data + collapse) in the DOM without breaking table structure
  - History status filter uses 'all' as sentinel value, converted to `undefined` when calling API
  - The `useNavigate` import was unused and had to be removed to pass TypeScript strict checks
  - `npm run build` produces 1040 KB (330 KB gzipped) — cron page adds minimal to bundle since it reuses MUI components
  - All 1373 existing tests still pass
---
