# Ralph Progress Log
Started: 2026-02-25
---

## Codebase Patterns
- Python source lives in src/taskrunner/ (package name is taskrunner, not creel yet)
- FastAPI daemon API is in src/taskrunner/daemon/api.py — use APIRouter for new endpoint groups
- Task models and loading in src/taskrunner/models.py — use load_all_tasks()
- Scheduler is APScheduler-based in src/taskrunner/scheduler.py
- NEW: Full cron system in src/taskrunner/cron/ (manager, store, models, executor, delivery, tool)
- Cron store already persists run history — check src/taskrunner/cron/store.py before building new history storage
- Config is agent.yaml (YAML), loaded as Pydantic models
- Telegram channel support added in src/taskrunner/channels/telegram.py
- Daemon service layer in src/taskrunner/daemon/service.py wraps business logic
- CREEL_HOME defaults to ~/.creel/ — state dir for daemon.sock, daemon.pid, daemon.log
- Tasks dir configurable, defaults to tasks/ relative to working directory
- Existing tests in tests/ — run with pytest
- pyproject.toml uses hatchling build system
- Bridge (FastAPI host bridge) is in src/bridge/ — separate from daemon
- Dashboard static dir path: `_DASHBOARD_STATIC_DIR` computed from `Path(__file__).resolve().parent.parent / "dashboard_static"` in api.py
- SPA fallback uses catch-all `/{full_path:path}` route — must be registered AFTER all other routes
- `_mount_dashboard()` is a no-op when dashboard_static/ dir is missing (graceful degradation)
- Tests run with `.venv/bin/python -m pytest tests/ -x -q`
- Dashboard scaffold: Vite 7 + React 19 + TypeScript 5.9 (strict mode enabled by default in template)
- Build script: scripts/build-dashboard.sh — npm ci + npm run build + copy dist/ to dashboard_static/
- Vite dev proxy: /api/* and /v1/* proxied to localhost:8099
- MUI v6 + Emotion installed; ThemeProvider in dashboard/src/ThemeContext.tsx wraps the app
- Dark mode toggle persists to localStorage key `creel-theme`; use `useThemeMode()` hook for mode/toggle
- Theme factory in dashboard/src/theme.ts — `buildTheme(mode)` creates MUI theme
- React Router v6 in dashboard/src/App.tsx — routes defined as children of Layout route
- BrowserRouter wraps the app in main.tsx
- Layout component in dashboard/src/Layout.tsx — AppBar (fixed) + responsive Drawer + Outlet
- Sidebar nav groups: Control, Workspace, System — defined as `NAV_GROUPS` array in Layout.tsx
- Page components in dashboard/src/pages/ — one file per page (OverviewPage.tsx, etc.)
- Drawer width: 240px; mobile breakpoint: `theme.breakpoints.down('md')`
- Dashboard API router in src/taskrunner/daemon/api_dashboard.py — prefix="/api", mounted via `app.include_router(dashboard_router)` in api.py
- Access DaemonService in endpoint handlers via `request.app.state.service`
- Agent definition accessible via `service._agent_def` (AgentDefinition model)

---

## 2026-02-25 - DASH-001
- Implemented static SPA serving from FastAPI daemon
- Files changed: src/taskrunner/daemon/api.py
- Added imports: Path, Request, FileResponse, StaticFiles
- Added `_DASHBOARD_STATIC_DIR` module-level constant
- Added `_mount_dashboard()` helper that:
  - Checks if dashboard_static/ dir and index.html exist before mounting
  - Mounts /assets via StaticFiles if assets/ subdir exists
  - Registers catch-all GET route for SPA fallback
  - Serves actual static files if they exist, otherwise returns index.html
- Called at end of `create_daemon_app()` after all API routes are registered
- **Learnings for future iterations:**
  - The catch-all route must be registered last so it doesn't shadow API routes
  - When dashboard_static/ is missing, all existing endpoints work unchanged
  - FastAPI StaticFiles requires the directory to exist at mount time, so we guard with is_dir()
  - All 1373 existing tests pass with no changes needed
---

## 2026-02-25 - DASH-002
- Scaffolded Vite + React + TypeScript frontend in dashboard/
- Files created: dashboard/ (Vite scaffold), scripts/build-dashboard.sh
- Files changed: .gitignore, vite.config.ts
- Scaffold created with `npm create vite@latest -- --template react-ts`
- Simplified App.tsx to minimal Creel Dashboard placeholder, removed template boilerplate (App.css, index.css, assets/)
- Configured Vite dev proxy: /api/* and /v1/* → localhost:8099
- TypeScript strict mode already enabled by Vite react-ts template in tsconfig.app.json
- Build script runs: npm ci → npm run build → copy dist/ to src/taskrunner/dashboard_static/
- Added dashboard/node_modules/, dashboard/dist/, src/taskrunner/dashboard_static/ to .gitignore
- npm run build produces output in dashboard/dist/ (verified)
- Build script copies to src/taskrunner/dashboard_static/ (verified)
- **Learnings for future iterations:**
  - Vite 7 react-ts template uses React 19 (not 18) — fine for our purposes
  - TypeScript strict mode is on by default in the template's tsconfig.app.json
  - `npm ci` is used in build script (not `npm install`) for reproducible installs
  - The `python -m pytest` command needs `.venv/bin/python` prefix (no system python)
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-003
- Installed MUI v6: @mui/material, @mui/icons-material, @emotion/react, @emotion/styled
- Created `dashboard/src/theme.ts` — `buildTheme(mode)` factory using `createTheme()` with primary/secondary palette colors and dark background overrides
- Created `dashboard/src/ThemeContext.tsx` — React context providing `mode` and `toggleTheme()`, wraps MuiThemeProvider + CssBaseline, persists preference to localStorage key `creel-theme`
- Updated `dashboard/src/main.tsx` — wraps `<App />` in `<ThemeProvider>`
- Updated `dashboard/src/App.tsx` — MUI AppBar with "Creel Dashboard" title and dark mode toggle IconButton (Brightness4/7 icons)
- Files created: dashboard/src/theme.ts, dashboard/src/ThemeContext.tsx
- Files changed: dashboard/package.json, dashboard/src/main.tsx, dashboard/src/App.tsx
- **Learnings for future iterations:**
  - MUI v6 installed fine alongside React 19
  - Use `<ThemeContext value={...}>` (React 19 style) instead of `<ThemeContext.Provider value={...}>`
  - CssBaseline in the ThemeProvider ensures consistent baseline styling in both modes
  - `npm run build` produces 316 KB gzipped bundle (101 KB gzipped) with MUI included
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-004
- Implemented layout shell with sidebar navigation and React Router
- Files created: dashboard/src/Layout.tsx, dashboard/src/pages/OverviewPage.tsx, TaskListPage.tsx, TaskDetailPage.tsx, TaskNewPage.tsx, CronPage.tsx, FilesPage.tsx, ConfigPage.tsx, LogsPage.tsx, NotFoundPage.tsx
- Files changed: dashboard/src/App.tsx, dashboard/src/main.tsx, dashboard/package.json
- Installed react-router-dom dependency
- Layout component with:
  - Fixed AppBar with "Creel" name, connection status Chip (green "Connected"), dark mode toggle
  - Permanent MUI Drawer (240px) on desktop, temporary Drawer on mobile (< md breakpoint)
  - Sidebar nav grouped: Control (Overview, Tasks, Cron Jobs), Workspace (Files, Config), System (Logs)
  - Active page highlighted via `selected` prop on ListItemButton with custom `isActive()` helper
  - `useMediaQuery` for responsive drawer variant switching
- React Router v6 routes: /, /tasks, /tasks/new, /tasks/:name, /cron, /files, /config, /logs, * (404)
- BrowserRouter wraps the entire app in main.tsx
- 404 page with "Go to Overview" button
- All placeholder pages render their page title as h4 Typography
- **Learnings for future iterations:**
  - React Router `<Route path="tasks/new">` must come before `<Route path="tasks/:name">` to avoid matching "new" as a name param
  - NavLink `selected` state needs custom logic — use `isActive()` helper checking pathname prefix for nested routes (e.g., /tasks/*)
  - AppBar `position="fixed"` with `zIndex: theme.zIndex.drawer + 1` keeps it above the drawer
  - Need `<Toolbar />` spacer in both drawer content and main content to offset fixed AppBar height
  - `npm run build` succeeds — bundle is 415 KB (132 KB gzipped) with router + layout added
  - All 1373 existing tests still pass
---

## 2026-02-25 - DASH-005
- Implemented GET /api/status endpoint returning daemon status, agent info, channels, task stats, cron info, and recent runs
- Files created: src/taskrunner/daemon/api_dashboard.py
- Files changed: src/taskrunner/daemon/api.py (imported and included dashboard_router)
- Created APIRouter with prefix="/api" in api_dashboard.py
- Endpoint reads from DaemonService.status(), agent_def, load_all_tasks(), and cron-history.jsonl
- Returns structured JSON: daemon (running, pid, uptime_seconds, socket), agent (name, model, provider), channels (with enabled/connected), tasks (total, scheduled), cron (enabled_jobs, total_jobs, next_run), recent_runs (last 5)
- Sensible defaults when no tasks/channels/history exist
- Router included in create_daemon_app() BEFORE webhook routes and SPA fallback
- **Learnings for future iterations:**
  - Dashboard API routes use `/api/*` prefix (via APIRouter prefix="/api"), distinct from existing `/v1/*` routes
  - The dashboard router must be included before `_mount_dashboard()` call since the SPA catch-all would shadow `/api/*` routes
  - `request.app.state.service` gives access to the DaemonService instance (set in lifespan)
  - `service._agent_def` provides agent config (name, model, provider)
  - Cron history JSONL path: `~/.creel/cron-history.jsonl` — doesn't exist yet (DASH-010 will create it)
  - `load_all_tasks()` can throw FileNotFoundError if tasks dir doesn't exist — wrap in try/except
  - All 1373 existing tests still pass
---
