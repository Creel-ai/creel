"""Creel Host Bridge - HTTP server for macOS-native tools.

This package provides a lightweight HTTP server that runs on the host
(not in Docker containers) and executes CLI commands like memo and
remindctl on behalf of containerized executors.
"""

__version__ = "1.0.0"
